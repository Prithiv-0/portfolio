# Portfolio (Redesigned)

This repository contains a redesigned portfolio for Prithiv Alagirisamy built with Vite + React + Framer Motion.

Quick start

```powershell
Set-Location 'N:\Projects\Portfolio\site'
npm install
npm run dev
Start-Process 'http://localhost:5174'
```
